#!/bin/bash

docker build \
    --build-arg BUILD_DATE=`date -u +"%Y-%m-%dT%H:%M%SZ"` \
    --build-arg BUILD_DATE=`git rev-parse --short HEAD` \
    --build-arg BUILD_DATE=`git config --get remote.origin.url` \
    -t $IMAGE_NAME
